sudo: required

services:
  - docker

env:
  - DOCKER_COMPOSE_VERSION=1.22.0

stages:
  - Unit test
  
#notifications:
#  slack: hora-da-hora:0nFQxr2PykmKhNkJ78xYJMOP

before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin

jobs:
  include:
    - stage: "Unit test"
      name: "Unit test"
      script:
        - make run-dc-tests
        - docker-compose -f docker-compose.yml -d
        - docker-compose run api_monitoria manage.py makemigrations
        - docker-compose run api_monitoria manage.py migrate
        - make unit-tests
        - docker-compose exec api_monitoria flake8
        - docker-compose exec api_monitoria codecov -t ${CODECOV_TOKEN}
        
after_success:
  - docker-compose down